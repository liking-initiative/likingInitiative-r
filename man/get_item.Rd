% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_item.R
\name{get_item}
\alias{get_item}
\title{One stimulus across every study that used it}
\usage{
get_item(item, version = "latest")
}
\arguments{
\item{item}{An item name, e.g. \code{"kitkat"}.}

\item{version}{Release version, or \code{"latest"}.}
}
\value{
An object of class \code{likingInitiative_item} with \code{data}, \code{datasets} and
\code{version}.
}
\description{
The cross-study view. Because the underlying studies use different response
scales, compare on \code{normalized_rating}, not \code{rating}.
}
\details{
Only the first rating phase of a repeated-phase dataset is included, so
those studies do not carry extra weight in a cross-study comparison.
}
\examples{
\donttest{
k <- get_item("kitkat")
k$data
}
}
