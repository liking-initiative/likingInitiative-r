% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_dataset.R
\name{get_dataset}
\alias{get_dataset}
\title{Download one or more datasets}
\usage{
get_dataset(dataset, version = "latest", timepoint = NULL)
}
\arguments{
\item{dataset}{A dataset code (\code{"leeholyoak2021"}), a unique prefix, a
dataset id, or a character vector of them.}

\item{version}{Release version, or \code{"latest"}.}

\item{timepoint}{Optional rating phase to keep.}
}
\value{
An object of class \code{likingInitiative_dataset} with elements \code{data} (a
tibble), \code{metadata}, \code{dataset_code} and \code{version}. For several datasets, a
named list of them.
}
\description{
Reads a dataset's ratings from the release, with the metadata needed to
interpret them.
}
\details{
Two datasets repeat their rating phase -- \code{leeholyoak2021} (phases 1-3) and
\code{leehare2023exp2} (phases 1-2). For those, \code{subject_id} and \code{item_id}
together are \strong{not} unique; include \code{timepoint}, or pass the \code{timepoint}
argument to take a single phase.
}
\examples{
\donttest{
d <- get_dataset("leeholyoak2021")
head(d$data)
d$metadata$rating_scale_max
cite(d)
}
}
